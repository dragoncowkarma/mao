# Antigravity Rules

**Always read and follow [AGENTS.md](../AGENTS.md) and [SKILL.md](../SKILL.md)
before starting any task.**

- `AGENTS.md` (repo root) — architecture rules, invariants, conventions, agent
  roles (the single source of truth).
- `SKILL.md` (repo root) — commands, verification workflow, step-by-step recipes.

Do not add project rules here; update the root files instead so every AI tool
stays in sync.
