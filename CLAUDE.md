# CLAUDE.md

Always read and follow the file below before starting any task:

@AGENTS.md

- `AGENTS.md` — architecture rules, invariants, conventions, agent roles, commands, verification workflow, step-by-step recipes (the single source of truth).

Do not duplicate their content here; update `AGENTS.md` instead so every AI tool
(Claude Code, Codex, Antigravity, …) stays in sync.
